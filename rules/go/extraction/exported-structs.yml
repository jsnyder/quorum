id: go-exported-type
language: go
rule:
  kind: type_declaration
  has:
    kind: type_spec
    all:
      - has:
          field: name
          regex: '^[A-Z]'
      - has:
          kind: struct_type
